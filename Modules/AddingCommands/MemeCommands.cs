using System.Collections.Generic;
using System.Threading.Tasks;
using Discord.Commands;

namespace SuccBot.Modules.AddingCommands
{

    public class MemeCommands : ModuleBase
    {
        //NEED TO USE BD

        // Dictionary<string, string> commands = new Dictionary<string, string>();

        // [Command("addcommand", RunMode = RunMode.Async)]
        // public async Task AddCommandAsync(string command, [Remainder]string text)
        // {
        //     commands.Add(command, text);
        // }

        // [Command("say", RunMode = RunMode.Async)]
        // public async Task SayCommandAsync(string command)
        // {
        //     await ReplyAsync(commands.ContainsKey(command).ToString());
        // }     
    }
}